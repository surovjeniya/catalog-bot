export enum Commands {
  catalog = 'catalog',
  start = 'start',
  personal = 'personal',
  register = 'register',
  'sign-in' = 'sign-in',
  'create-service' = 'createservice',
  signout = 'signout',
  message = 'message',
  support = 'support',
  chats = 'chats',
  ai = 'ai',
}
